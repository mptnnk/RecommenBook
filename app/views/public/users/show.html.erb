<!--mypage-->
<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'info', user: @user %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2 class="page-title">
        <% if @user == current_user %>
          マイページ
        <% else %>
          <%= @user.name %>さんのプロフィール
        <% end %>
      </h2>
      <p><%= link_to "いいね一覧仮置き場", likes_path %></p>
    </div>
    
  </div>
</div>